{
  "name": "project-memory-runtime",
  "version": "0.0.0",
  "private": true,
  "description": "Local-first lifecycle memory runtime for coding agents",
  "license": "GPL-3.0-only",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm --filter @slicenferqin/project-memory-runtime-core build",
    "test": "pnpm --filter @slicenferqin/project-memory-runtime-core test",
    "lint": "echo \"Lint pipeline pending\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
