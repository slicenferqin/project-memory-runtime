{
  "name": "project-memory-runtime",
  "version": "0.0.0",
  "private": true,
  "description": "Local-first lifecycle memory runtime for coding agents",
  "license": "GPL-3.0-only",
  "engines": {
    "node": ">=20 <21"
  },
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm --filter @slicenferqin/project-memory-runtime-core build",
    "benchmark:runtime": "pnpm run preflight:test && pnpm --filter @slicenferqin/project-memory-runtime-benchmarks benchmark",
    "memoryctl": "pnpm --filter @slicenferqin/project-memoryctl cli",
    "preflight:test": "node ./scripts/ensure-node-version.mjs",
    "rebuild:native": "pnpm rebuild better-sqlite3",
    "test": "pnpm run preflight:test && pnpm run rebuild:native && pnpm run typecheck && pnpm run lint && pnpm --filter @slicenferqin/project-memory-runtime-core test && pnpm --filter @slicenferqin/project-memoryctl test",
    "typecheck": "pnpm --filter @slicenferqin/project-memory-runtime-core typecheck",
    "lint": "pnpm exec eslint packages/runtime/src packages/runtime/tests tools/benchmarks tools/memoryctl --max-warnings=0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@typescript-eslint/eslint-plugin": "^8.57.0",
    "@typescript-eslint/parser": "^8.57.0",
    "eslint": "^10.0.3",
    "globals": "^17.4.0",
    "typescript": "^5.6.3"
  }
}
